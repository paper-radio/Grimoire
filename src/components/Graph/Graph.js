import React from "react";
import "./Graph.css";

const graph = () => {
    return (
        <div id="graph">
            <header>
                <p>Graph</p>
            </header>
        </div>
    );
};

export default graph;
